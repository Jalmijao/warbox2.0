spring:
  main:
    allow-bean-definition-overriding: true

warbox:
  storage:
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin123
    bucket: warbox-videos
    region: us-east-1
    default-chunk-size-bytes: 5242880

logging:
  level:
    root: INFO
    com.warbox: DEBUG
